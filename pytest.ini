[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
norecursedirs = .git __pycache__ *.egg-info .tox node_modules venv
markers =
    integration: tests that need a live MongoDB instance
